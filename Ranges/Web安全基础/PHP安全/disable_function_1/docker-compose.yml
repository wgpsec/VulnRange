version: '3'
services:
  web:
    build:
      context: ./
      dockerfile: Dockerfile
    image: antswordproject/antsword-labs:bypass_disable_functions_1
    container_name: disable_functions_1
    ports:
      - "80"
